# Article Source Archive

Store non-public article source Markdown files in this directory.

- Put publishable blog content in `_posts/`.
- Keep root-level Markdown files only for real public pages such as `company.md`.
- If a new root-level Markdown file is added by mistake, the Jekyll build will fail.
